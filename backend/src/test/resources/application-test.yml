spring:
  datasource:
    url: jdbc:h2:mem:fpe_common;MODE=PostgreSQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true
    locations: classpath:db/migration
app:
  attachment-storage-dir: ${java.io.tmpdir}/fpe-test-attachments
