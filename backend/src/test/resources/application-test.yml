spring:
  datasource:
    url: jdbc:h2:mem:faculty_eval;MODE=PostgreSQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: true
    locations: classpath:db/testmigration
app:
  session:
    cookie-secure: false
    ttl-hours: 8
